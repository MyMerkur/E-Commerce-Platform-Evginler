# api.evginlerevtekstil.com — Website-Evginler backend (Express, :4050)

server {
    listen 80;
    listen [::]:80;
    server_name api.evginlerevtekstil.com;

    location / {
        proxy_pass http://127.0.0.1:4050;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    # certbot --nginx bu bloğu otomatik ekleyip 443/SSL yönlendirmesi kuracaktır.
}
